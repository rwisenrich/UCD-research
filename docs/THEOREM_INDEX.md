# Theorem Index

## Finite gauge-matter representation

- Charge polynomial: `Q=(C^3-4C-3I)/18=B/2+A/3-I/6` for `C=2A+B`.
- Native matter representation: `(1,2)_{-1}`, `(1,1)_{-2}`, `(1,1)_0`, `(3,2)_{1/3}`, `(3,1)_{-2/3}`, `(3,1)_{4/3}` with three family copies.
- One-generation anomaly sums vanish.
- Boundary trace metric gives `G3:G2:GY=1:1:5/3`.

## Parent operator and normalization

- Exact Gram metric: `diag(12/7,2,12/7,12/7,1,1/3,4/7)`.
- Unique orthogonal projection: `Gc=J`, `J_a=Tr(T_a^dag K)/504`.
- Rescaling covariance: `G'=SGS`, `J'=SJ`, `c'=S^{-1}c`.

## Vacuum and family algebra

- Central right-Dic6 class functions restrict to a two-dimensional Hermitian algebra on the local ground doublet: identity plus one selector.
- `S_x=(1/12) sum_k R(a^k x)` has eigenvalues `{-1,+1}` on that doublet.
- `H_sel=-tau sigma S_x` gives a unique selected ground state with splitting `2 tau=1/36`.
- `Alg_C(I,X_Z,J_Z,D)=M3(C)` and its commutant is `C I`.

## Many-body addressed network

- `H504=H144 + H360 = H84 + H420` with simultaneous refinement ranks `24,120,60,300`.
- The five-detail fiber generates `M5(C)`; N independent fibers generate `M_{5^N}(C)`.

## Locality and continuum coordinates

- `Gamma_{r,s}=Z_{3 2^{r+s}} x Z_{2 2^{r+s}} x Z_{2 2^{r+s}}`.
- `a_r=L_*/2^r`, `dt_r=T_*/2^r`, and `a_r/dt_r=c`.
- A depth-n nearest-neighbor circuit has exact Heisenberg support radius at most n, giving `d_1 <= c t` after the coordinate map.
- The discrete Dirac symbol converges at order `a^2`.

## Relational reconstruction

- Rooted spanning-tree gauge fixes all tree links to identity; chord links are a fundamental-cycle holonomy basis.
- Informationally complete Hermitian expectations determine the density matrix uniquely.
- Rank-one density matrix determines a state vector up to global phase.
- Exact unitary history reconstructs the initial state by `Psi_0=U_{0:N}^dag Psi_N`.

## Regge and global volume functional

- `G=hbar c^5/(8 pi kappa_R E_*^2)`.
- `Lambda L_*^2=lambda_R/kappa_R`.
- `lambda_R^ind=-(1/N4) log(Z_Omega/Z_flat)`.
- Connected-cumulant identity: `lambda_R^ind=(1/N4) sum_{n>=1} (-1)^{n+1} kappa_n(Delta S)/n!`.
- A suppressive connected saddle family of degeneracy `g` and action `S0` contributes `g exp(-S0)+O(exp(-2 S0))`.

## Color and atomic spectra

- Exact meson singlet has `T_q . T_qbar=-4/3`.
- Exact baryon singlet has each pair `T_i . T_j=-2/3`.
- Stable spectral mass follows from the large-Euclidean-time logarithmic derivative of the singlet correlator.
- Principal-shell capacity is exactly `2 n^2`.

## Maxwell, RG, scalar, dark-sector, and continuum-gap extensions

- Addressed U(1) trace normalization: `Tr Q^2=156`, `S_p=(1/156) Re Tr(I-exp(iQ Theta_p))`.
- Finite-range inversion-symmetric hidden kernels are analytic and even at `k=0`; a leading `|k|^3` symbol requires a critical/nonanalytic Schur interior.
- One-loop coefficients in GUT normalization: `b1=41/10`, `b2=-19/6`, `b3=-7`; refinement gives `ln(mu_{r+1}/mu_r)=ln 2`.
- The minimal Hermitian degree-one family algebra is `span_R{I,X_Z,J_Z}` and the orientation-odd subspace is the one-dimensional line `R J_Z`.
- Gravitational coefficients are the orthogonal projection of the addressed parent functional onto curvature invariants modulo topological/boundary null directions.
- Frozen cosmological evaluation uses one model object `M=(Omega0,S_parent,E_*,history,nuisance_policy)` for every observable block; `chi^2=r^T C^{-1}r` is invariant under invertible observable-coordinate changes.
- For the Lemm nearest-neighbor frustration-free projector class, `gamma_N >= gamma_Bn-1/n-2/n^2`. Combined with `a_r H_phys=J_r K_r`, the sufficient physical certificate is `gamma_Bn >= 1/n+2/n^2+a_r m0/J_r`.
- A limiting positive Euclidean spectral measure obeying a uniform bound `|C_r(t)|<=A exp(-m0 t)` has no support in `(0,m0)`.
