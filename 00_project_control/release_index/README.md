# Release Index

This directory records frozen public releases across the monorepo: tag/commit, branch scope, manuscript set, executable set, verifier status, checksums, generated artifacts, and release notes.

A release record is immutable once published. Later repairs create a new release record linked to the superseded one.