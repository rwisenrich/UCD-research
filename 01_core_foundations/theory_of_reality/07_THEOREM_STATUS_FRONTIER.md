# 07 — Theorem Status and Current Frontier

The UCD Theory-of-Reality branch uses explicit status discipline. A mathematical construction is not promoted to a physical law merely because it is convenient downstream.

## Exact results

Current exact/constructed results include:

- the present relational floor admits explicit infinite and history-blind countermodels;
- finite branching plus well-founded realized refinement implies finite registry;
- the Q72 group law is exact once its registration premises are supplied;
- `[Q72,Q72]=<a^2>` has order 6 and the abelianized quotient has order 12;
- the C3 phase plane plus reversal induces an exact complex structure after real linearization;
- Q8 supplies an exact typed `1+1+6` role split;
- the `C3 x C2` six-boundary torsor has a unique connected homogeneous reversible `C6` step up to inversion, conditional on using one primitive full-support translation as adjacency;
- the named primitive Q72 generators define a canonical Cayley graph relative to that generator set;
- `H504=H84+H420` and the `1+5` quotient/detail split are exact once V7 is licensed;
- the ring-gap and Schur-inheritance identities are exact inside the stated Hamiltonian basis;
- legal H420 transport is an exact partial isometry and nontrivial holonomy does not imply norm loss;
- direct 420-to-84 linear compression cannot be injective; detail memory must be retained.

## Open upstream/physical-promotion gates

The current reduced frontier is:

- `P0-HMARK` — derive one genuinely history-sensitive lawful continuation with the required covariance;
- `P0-FINHAB` or `WFBOOL` — derive a genuinely global finiteness principle;
- `Q-ADD` — derive physical additive coherent alternatives;
- `Q-MEASNC` — derive projector/noncontextual orthogonally additive measurement semantics;
- `CELL-POS` — identify the exact typed `1+6` structural ports with the physical local-cell basis;
- `CELL-STEP` — promote the unique order-six full-support boundary translation to physical one-step adjacency;
- `NET-PRIM` — derive primitive registered operations as physical one-step links;
- `CONT-ORDER`, `CONT-MAN`, `CONT-VOL`, `CONT-DIM`, `CONT-SCALE` — derive the physical continuum, dimension and metric calibration.

## Truth-boundary rule

If a countermodel satisfies all current upstream premises while violating the desired conclusion, that conclusion is not hidden somewhere in the existing premise set. Progress then requires a genuinely new premise/theorem, not more algebraic manipulation of the same assumptions.

That rule is part of the canon discipline for this repository.