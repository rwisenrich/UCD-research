# Reproducibility

Cross-project Python runners, Colab notebooks, CI workflows, result certificates, deterministic regeneration commands, environment locks, and checksum ledgers.

Current root `scripts/`, `notebooks/`, `results/`, and GitHub Actions remain live while this monorepo layer is populated.